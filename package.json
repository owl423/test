{
  "scripts": {
    "sass": "node-sass --output-style expanded sass -o css",
    "watch": "npm run sass -- --watch --source-map-embed",
    "build": "node-sass --output-style compressed css/modules/grid.css css/modules/grid.min.css"
  },
  "devDependencies": {
    "node-sass": "^4.5.3"
  }
}
